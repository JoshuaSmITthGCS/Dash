import { Loading } from '../../../components/Bits.jsx'

/** The existing `<Loading/>` skeleton, ported as-is (DESIGN.md §12 empty/loading/error). */
export default function Skeleton() {
  return <Loading />
}
